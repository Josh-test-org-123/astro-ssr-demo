// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // output: 'static', // Astro's default is static, so this line is optional
});