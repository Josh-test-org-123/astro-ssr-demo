{
  "name": "astro-ssr-demo",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && cp src/_worker.js ./_worker.js",
    "preview": "astro build && cp src/_worker.js ./_worker.js && npx wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && npx wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "astro": "^5.8.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "wrangler": "^4.19.0"
  }
}